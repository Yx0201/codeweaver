# 混合检索功能开发

### 需求背景
 - 现在的知识库检索准确率,共程度,召回率,相关性等等的成绩都太差了,因为目前编写了一个向量检索,还缺少检索优化,比如混合检索,重排序等等优化,所以我们需要添加这些功能

### 修改流程
 - 首先将现在的向量检索,拆分成向量检索和关键词检索(BM25)他们并行各自检索20条,然后汇总合并,在使用融合排序返回相近的10个
 - 为了应对这个需求,我们的数据库也需要改造一下,现在不支持关键词检索,将这次修改涉及到的数据库改动一起同步执行
 -  